{% extends 'base.html.twig' %}

{% block title %}
    {{ title }}
{% endblock %}

{% block body %}
    <h1>{{ title }}</h1>
    <h3>Comments {{ comments|length }}</h3>
        {% for comment in comments %}
            <li>{{ comment }}</li>
        {% endfor %}
{% endblock %}